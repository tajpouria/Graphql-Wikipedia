export enum WikipediaApiLanguage {
    en = "en",
    nl = "nl",
}

export enum Actions {
    opensearch = "?action=opensearch&",
}

export enum ResponseDateFormat {
    "json" = "json",
    "jsonfm" = "jsonfm",
    "xml" = "jsonfm",
    "xmlfm" = "xmlfm",
}
